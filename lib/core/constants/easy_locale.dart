import 'dart:ui';

import 'package:perfetto/core/constants/app_constants.dart';

class EasyLocale {
  static final all = [
    Locale(AppConstants.en),
    Locale(AppConstants.ru),
    Locale(AppConstants.uz),
  ];
}
